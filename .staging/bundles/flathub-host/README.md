# Flathub host (`scripts/flathub-host/`)

Host-only helper: runs **`flatpak run $FLATHUB_APP_ID`** when the **`flathub-host`** workflow is used.

Canonical script: **`assets/scripts/flathub-host-run.sh`**. Workflows reference it as **`scripts/flathub-host/flathub-host-run.sh`** (see `paths.go`).
