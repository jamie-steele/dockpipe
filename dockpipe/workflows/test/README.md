# test

**Purpose:** Minimal **multi-step** workflow — step 1 writes **`.dockpipe/outputs.env`**, step 2 reads merged env (see **[docs/workflow-yaml.md](../../docs/workflow-yaml.md)**).

```bash
dockpipe --workflow test
```

Needs Docker + **`alpine`**.
