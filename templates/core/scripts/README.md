# Shared scripts (optional)

Most host/container scripts live at the repository **`scripts/`** root (e.g. **`clone-worktree.sh`**, **`commit-worktree.sh`**). Strategies and workflows reference those paths.

Use this folder only for **template-local** shared snippets if you maintain a fork; the bundled tree usually leaves this empty aside from this README.
