# Shared images (optional)

Dockerfiles and image dirs usually live under the repository **`images/`** tree. Resolver **`DOCKPIPE_RESOLVER_TEMPLATE`** names (e.g. **`claude`**, **`codex`**) point at those builds.

This folder is reserved for **template-local** shared image notes or assets in custom setups.
