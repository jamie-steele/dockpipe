# Optional example — not executed by dockpipe. Build image first from templates/core/assets/images/codex/
services:
  codex:
    image: dockpipe-codex:latest
    working_dir: /work
    # environment:
    #   OPENAI_API_KEY: ${OPENAI_API_KEY}
    #   CODEX_API_KEY: ${CODEX_API_KEY:-${OPENAI_API_KEY}}
