# Resolvers

Add resolver files here (e.g. `claude`, `codex`). When you run `dockpipe init <template-name>`, the init code copies example resolvers from the dockpipe repo so this template works out of the box. Add or edit resolver files to customize.
