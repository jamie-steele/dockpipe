# Init template

Copied to **`templates/<name>/`** when you run **`dockpipe init <name>`**, together with example **`scripts/`** and **`images/`** from the scaffold.

Edit **`config.yml`** to match your workflow. **Learning path:** **[docs/onboarding.md](../../docs/onboarding.md)**.
