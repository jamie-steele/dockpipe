# DorkPipe workflows (tracked)

These **`config.yml`** trees ship with the **`dorkpipe`** module (`src/lib/dorkpipe/`) — orchestrator dogfood, self-analysis, compliance handoff — not under **`.staging/`**. **`--workflow`** names are unchanged (e.g. **`dorkpipe-self-analysis`**). See **`docs/dorkpipe.md`**.
