# Reference resolver-shaped workflow — see README.md in this directory.
name: example
category: demo
description: Minimal layout demo for resolver profiles (agnostic script only).

steps:
  - id: notice
    run: scripts/core.assets.scripts.example-resolver-notice.sh
    kind: host
