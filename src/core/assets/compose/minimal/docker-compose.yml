# Agnostic example 1: single long-running service (not run by dockpipe).
# docker compose -f templates/core/assets/compose/minimal/docker-compose.yml up
services:
  sleep:
    image: alpine:3.20
    command: ["sleep", "infinity"]
