# Pipeline: run → apply (pair with run-apply-validate when you need a third validate phase)
name: run-apply
description: Two sequential steps — run phase, then apply phase.

steps:
  - id: run
    runtime: dockerimage
    isolate: alpine
    cmd: sh -c 'echo "[run] phase" >&2'
  - id: apply
    runtime: dockerimage
    isolate: alpine
    cmd: sh -c 'echo "[apply] phase" >&2'
