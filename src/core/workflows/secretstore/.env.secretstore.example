# Copy to .env.secretstore in your project root (--workdir). Standard KEY=value lines (POSIX shell).
# Do not commit real secrets — this file is usually gitignored as .env.secretstore
#
# Example:
#   export MY_API_TOKEN=replace-me
#   CLOUDFLARE_ACCOUNT_ID=your-account-id

MY_API_TOKEN=replace-me
