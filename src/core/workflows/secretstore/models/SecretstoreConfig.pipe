/// <summary>
/// Concrete defaults for the bundled secretstore workflow.
/// </summary>
public Class SecretstoreConfig : ISecretstoreConfig
{
    public string SecretstoreProvider = "dotenv";
    public string SecretstoreEnvFile = ".env.secretstore";
}
