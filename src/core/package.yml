schema: 1
name: dockpipe.core
title: DockPipe Core
platforms: [windows, deb]
description: Core runtime assets shipped with DockPipe.
kind: core
build:
  source:
    script: assets/scripts/build-dockpipe-guest-agent.sh
