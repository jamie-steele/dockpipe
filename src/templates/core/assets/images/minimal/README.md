# Minimal (`minimal/`)

Tiny **Debian** image with **`assets/entrypoint.sh`** — a **second agnostic** `TemplateBuild` example alongside **`example/`**. Not registered in **`template.go`** by default; use as a copy-paste pattern or register a name if you need **`--isolate minimal`**.
