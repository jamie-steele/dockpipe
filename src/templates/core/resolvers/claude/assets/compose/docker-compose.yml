# Optional example — not executed by dockpipe. Build image first from templates/core/resolvers/claude/assets/images/claude/
services:
  claude:
    image: dockpipe-claude:latest
    working_dir: /work
    # environment:
    #   ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
    #   CLAUDE_API_KEY: ${CLAUDE_API_KEY:-${ANTHROPIC_API_KEY}}
