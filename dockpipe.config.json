{
  "schema": 1,
  "compile": {
    "workflows": [
      "workflows",
      "packages",
      ".staging/packages"
    ]
  },
  "packages": {
    "namespace": "dockpipeproject"
  },
  "secrets": {
    "op_inject_template": ".env.op.template",
    "notes": "Optional: 1Password op inject mapping (op:// lines). Use with secretstore-onepassword / secretstore-r2-publish-test; template example under packages/secrets/resolvers/onepassword/. Bundled core secretstore uses .env.secretstore (dotenv) — see src/core/workflows/secretstore/."
  }
}
