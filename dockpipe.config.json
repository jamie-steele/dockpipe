{
  "schema": 1,
  "compile": {
    "workflows": [
      "workflows",
      "packages",
      ".staging/packages"
    ]
  },
  "packages": {
    "namespace": "dockpipeproject"
  },
  "secrets": {
    "vault_template": ".env.op.template",
    "notes": "Optional: op inject env file (op:// lines). vault_template can be .env.op.template or .env.vault.template."
  }
}
