{
  "schema": 1,
  "compile": {
    "workflows": [
      "workflows",
      "packages",
      ".staging/packages"
    ]
  },
  "packages": {
    "namespace": "dockpipeproject"
  },
  "secrets": {
    "vault_template": ".env.op.template",
    "vault": "op",
    "notes": "vault_template: op inject env file. secrets.vault: default when workflows omit vault:."
  }
}
