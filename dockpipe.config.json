{
  "schema": 1,
  "compile": {
    "workflows": [
      "workflows",
      "packages",
      ".staging/packages"
    ]
  },
  "packages": {
    "namespace": "dockpipeproject"
  },
  "secrets": {
    "vault_template": ".env.template",
    "vault": "op",
    "notes": "vault_template: env mapping file (agnostic name). With vault op, use op inject (op://). Other vault backends can substitute the same keys."
  }
}
