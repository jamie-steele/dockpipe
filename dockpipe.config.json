{
  "schema": 1,
  "compile": {
    "workflows": [
      "workflows",
      "packages",
      ".staging/packages"
    ]
  },
  "packages": {
    "namespace": "dockpipeproject"
  },
  "secrets": {
    "vault_template": ".env.vault.template",
    "notes": "Optional: vault env template (op:// lines when using vault: op — op inject today). Same file can be listed as op_inject_template for older configs. Bundled secretstore dotenv: src/core/workflows/secretstore/."
  }
}
