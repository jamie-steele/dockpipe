{
  "schema": 1,
  "compile": {
    "workflows": [
      "workflows",
      "src/lib/dorkpipe/workflows",
      ".staging/packages"
    ],
    "bundles": [
      ".staging/packages/dockpipe"
    ],
    "resolvers": [
      "src/core/resolvers",
      ".staging/packages"
    ]
  },
  "packages": {
    "namespace": "dockpipeproject"
  },
  "secrets": {
    "op_inject_template": ".env.op.template",
    "notes": "Optional: 1Password op inject mapping (op:// lines). Use with secretstore-onepassword / secretstore-r2-publish-test; template example under .staging/packages/dockpipe/secrets/resolvers/onepassword/. Bundled core secretstore uses .env.secretstore (dotenv) — see src/core/workflows/secretstore/."
  }
}
