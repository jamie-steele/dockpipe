# Resolver: ollama

**Isolate template:** **`ollama`** → builds **`dockpipe-ollama`** from **`assets/images/ollama/`** under this resolver.

Use for workflows that run **`ollama serve`** and host-side or in-container model steps (for example the repo-root **`workflows/review-pipeline/`** dogfood flow in this checkout).
