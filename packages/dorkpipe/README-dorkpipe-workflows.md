# DorkPipe workflows (index)

DorkPipe maintainer workflows are grouped in **one plugin directory**: **`dockpipe/dorkpipe/`** with **`resolvers/<name>/`** for each shipped workflow (orchestrator, self-analysis, compliance-handoff, …).

See **`dorkpipe/README.md`** inside that folder.
