[package]
name = "pipeon-desktop"
version = "0.6.0"
edition = "2021"
description = "Pipeon desktop shell"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
arboard = "3"
tauri = { version = "2", features = [] }
url = "2"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
