# `ide` package tests

- **`test_pipeon.sh`** — Pipeon CLI gate + bundle smoke (`ide/bin/pipeon`).

Run from repository root:

```bash
bash packages/pipeon/tests/run.sh
# or
bash packages/pipeon/tests/test_pipeon.sh
```
