# Primitives (renamed)

This topic is documented as **capabilities** — stable dotted ids for abstract needs vs concrete resolver packages.

**→ See [capabilities.md](capabilities.md)** for terminology, YAML keys (`capability:`, `requires_capabilities:`), and rules.

The old YAML keys **`primitive:`** and **`requires_primitives:`** are still accepted as aliases.
