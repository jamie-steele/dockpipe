# Docs TODO

The indexed backlog lives under `docs/todos/`.

Start at:

- `docs/agents/todo-index.yaml`

Closed history lives under:

- `docs/todos/closed/`

Keep the index and the linked topic files current when work materially completes or advances one of
those items.
Do not keep closed items in the active YAML index.
