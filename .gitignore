# Build output
packaging/build/
*.deb
bin/dockpipe.bin
bin/dockpipe.exe
# ad-hoc go build -o in repo root (prefer: make build-windows → bin/dockpipe.exe)
/dockpipe.exe
msi-dist/
*.wixobj
*.wixpdb
dist/

# Local demo recordings (asciinema + agg)
demo/dockpipe-demo*.cast
demo/dockpipe-demo*.gif

# OS
.DS_Store
Thumbs.db

# Editor / temp
*~
*.swp
*.swo
.*.sw?

# Optional local overrides (do not commit)
.env
.env.local

# DorkPipe self-analysis scratch facts (handoff markdown may be kept locally)
.dockpipe/self-analysis/

# CI scan normalization (local / optional; CI uploads as workflow artifacts)
.dockpipe/ci-raw/
.dockpipe/ci-analysis/

# User insight queue + normalized insights (local; optional commit of curated insights.json)
.dockpipe/analysis/

# Pipeon generated context bundle (local)
.dockpipe/pipeon-context.md

# VS Code extension (packed / dev)
contrib/pipeon-vscode-extension/node_modules/
contrib/pipeon-vscode-extension/*.vsix
